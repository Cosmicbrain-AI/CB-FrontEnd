# Copy this file to .env and fill in values

# Required: Gemini API key from AI Studio
GOOGLE_API_KEY=YOUR_API_KEY_HERE

# Optional: default model (2.5 series recommended)
GEMINI_MODEL=gemini-2.5-flash

# Optional: global system prompt override
# SYSTEM_PROMPT=You are a Robotics Training Assistant for a platform that converts user-recorded task demonstrations into VLA policies...

# Optional: CORS origins (comma-separated). Use * for development
CORS_ALLOW_ORIGINS=*

# Optional: per-request model override used by tests (example)
# MODEL_OVERRIDE=gemini-2.5-pro

